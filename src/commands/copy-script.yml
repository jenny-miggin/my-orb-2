description: >
  This command copies the my-script file to tmp
# What will this command do?
# Descriptions should be short, simple, and clear.

steps:
  - run:
      name: Copy script
      command: <<include(scripts/copy-script.sh)>>